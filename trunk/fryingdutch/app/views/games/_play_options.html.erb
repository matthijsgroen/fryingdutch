<%
	game_rating = current_user.game_ratings.find_by_game_id game.id	
%>
	<p class="rating_label">Mijn mening:</p>
	<div class="userrating box editable">
		<div class="userrating score" style="width: <%=game_rating.rating * 18 rescue 0 %>px;"></div>
	</div>
	<p>
		<%= link_to "Ik speel dit spel ook", user_add_game_path(current_user, game), :rel => "remote" unless current_user.games_playing.include? game %>
		<%= link_to "Ik ben gestopt met dit spel", user_remove_game_path(current_user, game), :rel => "remote" if current_user.games_playing.include? game %>
	</p>