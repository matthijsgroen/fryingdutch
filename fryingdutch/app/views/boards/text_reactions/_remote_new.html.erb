<% form_for @text_reaction, :url => board_text_reactions_path(@board, @topic), :html => { :method => :post } do |f| %>
		<%= f.text_area :comment, :rows => 10 %><br />
		<%= f.submit "Toevoegen" %>
<% end %>